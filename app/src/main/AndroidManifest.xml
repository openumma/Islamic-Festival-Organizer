<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ummahackathon">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="30" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UmmaHackathon">
        <activity android:name=".ui.SpalshScreen.SpalashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.Video.VideoActivity" />
        <activity android:name=".ui.Jadwal.JadwalPengisiAcaraActivity" />
        <activity android:name=".ui.Detail.DetailHadrohActivity" />
        <activity android:name=".ui.Detail.DetailQoriActivity" />
        <activity android:name=".ui.Detail.DetailUstadActivity" />
        <activity android:name=".ui.ReportPendaftaran.ReportPendaftaran" />
        <activity android:name=".ui.Pendaftaran.FormPendaftaran" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.UmmaHackathon.NoActionBar"/>
    </application>

</manifest>